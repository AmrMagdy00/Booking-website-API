import { RefreshRotationStrategy } from './refresh-rotation.strategy';

describe('RefreshRotationStrategy', () => {
  it('should be defined', () => {
    expect(new RefreshRotationStrategy()).toBeDefined();
  });
});
