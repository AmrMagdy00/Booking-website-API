/**
 * BookingStatus - Enum for booking status values
 * Defines the possible states a booking can be in
 */
export enum BookingStatus {
  PENDING = 'pending',
  CANCELED = 'canceled',
  CONFIRMED = 'confirmed',
}

